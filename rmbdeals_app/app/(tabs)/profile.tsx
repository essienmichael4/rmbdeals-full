import React from 'react';
import ProfileScreen from '../../screens/main/ProfileScreen';

export default function Profile() {
  return <ProfileScreen navigation={undefined as any} route={undefined as any} />;
}
